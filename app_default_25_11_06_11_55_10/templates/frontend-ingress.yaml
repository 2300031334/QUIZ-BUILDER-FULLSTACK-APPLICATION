{{- if .Values.frontend.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "quiz-builder.frontend.name" . }}-ingress
  labels:
    {{- include "quiz-builder.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/ingress.class: {{ .Values.frontend.ingress.ingressClassName | default "nginx" }}
    {{- if .Values.frontend.ingress.tls.enabled }}
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    {{- end }}
spec:
  rules:
    - host: {{ .Values.frontend.ingress.host }}
      http:
        paths:
          - path: {{ .Values.frontend.ingress.path | default "/" }}
            pathType: Prefix
            backend:
              service:
                name: {{ include "quiz-builder.frontend.name" . }}
                port:
                  number: {{ .Values.frontend.service.port }}
  {{- if .Values.frontend.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.frontend.ingress.host }}
      secretName: {{ .Values.frontend.ingress.tls.secretName }}
  {{- end }}
{{- end }}
